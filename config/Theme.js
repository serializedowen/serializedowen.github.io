const colors = {
  primary: '#7f0034', // Color for buttons or links
  secondary: '#efdfd0',
  bg: 'white', // Background color
  grey: {
    dark: 'rgba(0, 0, 0, 0.9)',
    default: 'rgba(0, 0, 0, 0.7)',
    light: 'rgba(0, 0, 0, 0.5)',
    ultraLight: 'rgba(0, 0, 0, 0.25)',
  },
};
const boxShadow = '0px 8px 16px 0px rgba(0, 0, 0, 0.2)';

const transitions = {
  normal: '0.5s',
};

const fontSize = {
  small: '0.9rem',
};

const theme = {
  colors,
  transitions,
  fontSize,
  boxShadow,
};

export default theme;
